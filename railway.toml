[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# Sin healthcheck: durante el import (1-3 h) no hay API en /status y Railway mataba el contenedor.
